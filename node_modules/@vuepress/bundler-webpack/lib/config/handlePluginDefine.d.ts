import type { App } from '@vuepress/core';
import type * as Config from 'webpack-chain';
/**
 * Set webpack DefinePlugin
 */
export declare const handlePluginDefine: ({ app, config, isBuild, isServer, }: {
    app: App;
    config: Config;
    isBuild: boolean;
    isServer: boolean;
}) => Promise<void>;
