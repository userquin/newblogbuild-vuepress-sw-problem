// export const formateDateString = (date) => {
//     const dateJson = new Date(date).toJSON();
//     const dateStr = new Date(+new Date(dateJson) + 8 * 3600 * 1000)
//         .toISOString()
//         .replace(/T/g, " ")
//         .replace(/\.[\d]{3}Z/, "");
//     return dateStr.split(" ")[0];
// };

export const formateDateString = (date) => {
    var d = new Date(date),
        month = '' + (d.getMonth() + 1),
        day = '' + d.getDate(),
        year = d.getFullYear();

    if (month.length < 2) 
        month = '0' + month;
    if (day.length < 2) 
        day = '0' + day;

    return [day, month, year].join('-');
}

export const resolveDate = (date, type) => {
    // const dateStr = formateDateString(date).replace(/-/g, "/");
    const dateObj = new Date(new Date(date).toJSON());
    const info = {
        day: dateObj.getDate(),
        month: dateObj.getMonth() + 1,
        year: dateObj.getFullYear()
    };
    return info[type].toString();
};
